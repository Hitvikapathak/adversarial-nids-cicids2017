# GitHub Repository — Published

**Live URL:** https://github.com/Hitvikapathak/adversarial-nids-cicids2017

The project has been pushed to GitHub. Use this URL in your IITK B.Cyber application.

## Repository settings (recommended)

1. **About** (top-right of repo page):
   - Description: `Adversarial robustness in ML-based NIDS using CIC-IDS2017`
   - Topics: `cybersecurity`, `machine-learning`, `adversarial-ml`, `intrusion-detection`, `cic-ids2017`

2. **Pin** this repo on your GitHub profile.

## What is on GitHub

| Folder/File | Purpose |
|-------------|---------|
| `README.md` | Project overview + reproduce steps |
| `LICENSE` | MIT license |
| `requirements.txt` | Python dependencies |
| `run.py` | One-command pipeline |
| `src/` | All source code |
| `scripts/` | Report generation + publish script |
| `notebooks/` | Jupyter notebook guides |
| `docs/` | Reflection + demo script |
| `results/metrics.json` | Exact experiment numbers |
| `results/*.png` | Confusion matrices + graphs |
| `results/*.csv` | Summary tables |
| `data/processed/dataset_profile.json` | Dataset statistics |
| `deliverables/` | Report PDF + one-pager copies |

## Not on GitHub (auto-regenerated)

- `data/raw/*.csv` — downloads on first `python run.py`
- `models/*.joblib` — regenerated by pipeline

## Update and re-push

```powershell
$env:Path = "C:\Program Files\Git\cmd;" + $env:Path
cd C:\Users\HP\adversarial-nids-cicids2017
git add .
git commit -m "Update project"
git push
```